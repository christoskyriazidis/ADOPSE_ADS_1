<UserControl x:Class="WpfClientt.views.filters.FilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfClientt.views.filters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200">
    <Border Padding="5">
        <Grid>
            <Grid.RowDefinitions >
                <RowDefinition Height="0.89*"/>
                <RowDefinition Height="0.05*"/>
                <RowDefinition Height="0.01*"/>
                <RowDefinition Height="0.05*"/>

            </Grid.RowDefinitions>
            <ItemsControl ItemsSource="{Binding FilterMemebers}" Grid.Row="0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel IsItemsHost="True" Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
            </ItemsControl>    
            <Button Content="Search" Grid.Row="1" Style="{StaticResource medium_blue_button}" />
            <Button Content="Reset" Grid.Row="3" Style="{StaticResource medium_red_button}" />
        </Grid>
    </Border>
    
</UserControl>
