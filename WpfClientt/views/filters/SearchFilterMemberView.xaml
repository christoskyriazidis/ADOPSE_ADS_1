<UserControl x:Class="WpfClientt.views.filters.SearchFilterMemberView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfClientt.views.filters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions >
            <ColumnDefinition Width="0.4*" />
            <ColumnDefinition Width="0.6*" />
        </Grid.ColumnDefinitions>
        <Border BorderThickness="0,0,1,0" BorderBrush="{StaticResource blue-medium}" Grid.Column="0" Margin="0,0,0,5">
            <TextBlock Text="{Binding Title}" FontSize="24" TextAlignment="Center" VerticalAlignment="Center" />
        </Border>
        <TextBox Grid.Column="1" VerticalAlignment="Center" Text="{Binding SearchQuery}" />
    </Grid>
</UserControl>
