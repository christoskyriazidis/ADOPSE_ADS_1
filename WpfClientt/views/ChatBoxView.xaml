<UserControl x:Class="WpfClientt.views.ChatBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfClientt.views"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.2*" />
            <ColumnDefinition Width="0.8*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.20*" />
            <RowDefinition Height="0.80*" />
        </Grid.RowDefinitions>
        <Image Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Margin="10" Source="{Binding Customer.ProfileImageUri}" />

        <TextBlock Grid.Row="0" Grid.Column="1" TextAlignment="Center">
            <Run Text="{Binding Customer.FirstName}"/>
            <Run Text=" " />
            <Run Text="{Binding Customer.LastName}" />
        </TextBlock>
        <TextBlock Grid.Row="1" Grid.Column="1" Margin="10"  VerticalAlignment="Top" HorizontalAlignment="Left">
            <Run Text="Last Message: "/>
            <Run Text="{Binding LatestMessage}" />
        </TextBlock>
    </Grid>
</UserControl>
